/*************** LEER *****************/

En la carpera src ejecute en la terminal lo siguiente:

1. mpiCC -g -Wall -o "Nombre" "Nombre.cpp"
2. mpiexec -n "numero de procesos" "Nombre"
3. el tamaño de la fila se define con el valor de "numero de procesos".
4. digite el numero de columnas, que no debe ser mayor al numero de filas.
